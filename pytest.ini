[pytest]
# The API smoke suite owns a temporary database and must run in a fresh Python
# process. test_prd5_full is a legacy black-box suite for a separately running
# PRD5 server and is superseded by the PRD6 milestone integration tests.
addopts = --ignore=tests/test_api_smoke.py --ignore=tests/test_prd5_full.py
